{
  "private": true,
  "name": "streaming-tools-client",
  "version": "0.0.0",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "build": "preact build --no-prerender --no-sw --no-esm --no-json --no-inline-css",
    "dev": "preact watch --no-prerender --no-sw --no-esm --no-json "
  },
  "devDependencies": {
    "preact-cli": "3.3.3"
  },
  "dependencies": {
    "classnames": "2.3.1",
    "leapjs": "1.1.1",
    "nth-check": "2.0.1",
    "preact": "10.6.4",
    "preact-render-to-string": "5.1.19",
    "socket.io-client": "4.4.1"
  }
}
