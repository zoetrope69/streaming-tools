{
  "private": true,
  "name": "streaming-tools-client",
  "version": "0.0.0",
  "scripts": {
    "build": "preact build --no-prerender --no-sw --no-esm --no-json --no-inline-css --verbose",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "devDependencies": {
    "preact-cli": "^3.0.0"
  },
  "dependencies": {
    "classnames": "^2.3.1",
    "preact": "^10.3.2",
    "preact-render-to-string": "^5.1.4",
    "socket.io-client": "^4.1.2"
  }
}
